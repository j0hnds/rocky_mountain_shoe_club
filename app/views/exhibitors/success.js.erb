$('#record_manager').fadeOut('fast',function() {
  $(this).empty();
  $('#module_messages')
      .html("<%=escape_javascript(render_stickies)%>")
      .pulse({
              callback : function(){
                      $('#exhibitors_list')
                              .css('display','none')
                              .html("<%= escape_javascript(render(:partial => "exhibitors_list")) %>")
                              .fadeIn('fast');
              }
      });
});