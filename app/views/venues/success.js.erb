$('#record_manager').fadeOut('fast',function() {
  $(this).empty();
  $('#module_messages')
      .html("<%=escape_javascript(render_stickies)%>")
      .pulse({
              callback : function(){
                      $('#venues_list')
                              .css('display','none')
                              .html("<%= escape_javascript(render(:partial => "venues_list")) %>")
                              .fadeIn('fast');
              }
      });
});