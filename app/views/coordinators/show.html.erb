<% form_for @coordinator, :html => { :method => :delete, :class => 'auxiliary ajaxed' } do |f| %>
<p><strong>Delete exhibitor: <%= format_name(@coordinator) -%>?</strong></p>
  <% field_set_tag nil, :class => "button_wrapper" do %>
    <%= f.submit 'Delete', :class => "button" -%>
  <% end %>
<% end %>