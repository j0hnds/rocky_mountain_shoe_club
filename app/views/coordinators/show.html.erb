<% form_for @coordinator, :html => { :method => :delete, :class => 'auxilliary ajaxed' } do |f| %>
<ol>
  <li>Delete exhibitor: <%= format_name(@coordinator) -%></li>
  <li><%= f.submit -%></li>
</ol>
<% end %>